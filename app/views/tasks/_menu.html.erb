<ul class="task-menu">
  <% if can? :destroy, task %>
    <li><%= link_to icon(:destroy), task_path(task), :method => :delete %></li>
  <% end %>
  <% if can? :update, task %>
    <li><%= link_to icon(:edit), edit_task_path(task) %></li>
  <% end %>
</ul>
